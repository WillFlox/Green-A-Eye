# Variables de entorno para Green A-Eye

# Frontend (Next.js)
# URL del backend API
NEXT_PUBLIC_API_URL=http://localhost:8000

# Backend (FastAPI)
# Orígenes permitidos para CORS (separados por comas)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Puerto del backend (algunas plataformas lo configuran automáticamente)
PORT=8000

